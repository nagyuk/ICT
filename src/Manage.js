import React from 'react';
import { Box, Heading, Text } from '@chakra-ui/react';

const Manage = () => {
  return (
    <Box p={5}>
      <Heading as="h2" mb={4}>管理画面</Heading>
      <Text>ここに管理画面の内容を表示します。</Text>
    </Box>
  );
};

export default Manage;